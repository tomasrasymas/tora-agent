[project]
name = "tora-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.104.1",
    "openai>=2.38.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
